{
  "name": "hki-vesiliikenne",
  "version": "1.0.0",
  "repository": "git@github.com:reaktor/hki-vesiliikenne.git",
  "license": "UNLICENSED",
  "scripts": {
    "build": "tsc",
    "fetch": "yarn build && node --trace-warnings --unhandled-rejections=strict build/create-routes-json.js && cat routes.json"
  },
  "dependencies": {
    "axios": "^0.19.2",
    "leaflet-polylineoffset-lc": "^1.2.0"
  },
  "devDependencies": {
    "typescript": "^3.9.3",
    "@types/node": "^14.0.5"
  }
}
